h3 Bài viết liên quan
if (data.relatedPosts && data.relatedPosts.length > 0)
	.related-post-list
		ul
			each thePost, i in data.relatedPosts1
				li
					span.post-title
						a(href=thePost.slug + '.html')= thePost.title
